# behave.ini
# Behave configuration — controls default output formats

[behave]
# Default format when running locally
format   = pretty

# Stop on first failure locally (comment out for CI to run all tests)
# stop = true

# Paths to feature files
paths = features/

# Show step timings
show_timings = true

# Suppress skipped steps output
show_skipped = false
